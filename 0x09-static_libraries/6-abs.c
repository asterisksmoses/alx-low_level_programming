#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the input value integer
 * Return: the absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
