#include "main.h"

/**
 * _islower - Function that checks for lowercase character
 * @c: The character to be checked
 * Return: 1if c is a lowercase character, otherwise 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
