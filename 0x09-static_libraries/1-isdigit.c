#include "main.h"

/**
 * _isdigit - function that checks for a digit
 * @c: input value in check
 * Return: 1 if c is a digit, 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
