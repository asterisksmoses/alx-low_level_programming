#include "main.h"

/**
 * _isdigit - Function that checks for a digit
 * @c: the int to be checked
 * Return: 1 where c is a digit 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
