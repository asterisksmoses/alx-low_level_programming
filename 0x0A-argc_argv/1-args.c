#include "main.h"
#include <stdio.h>

/**
 * main - function that calculates the number of arguments passed onto it
 * @argc: The number of arguments
 * @argv: Arrays of arguments
 * Return: 0 each time
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc - 1);
	{
		return (0);
	}
}
