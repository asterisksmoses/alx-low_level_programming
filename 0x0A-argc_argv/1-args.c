#include "main.h"

/**
 * main - prints the numer of arguments passed to it
 * @argc: number of arguments
 * @agrv: an array of arguments
 *
 * Return: 0 each time
 */

int main(int agrc char *agrv[])
{
	(void) argv; /*Ignore argv*/
	printf("%d\n", argc - 1);

	return (0);
}
